workflows:
  android-debug:
    name: Tsega Android Debug
    instance_type: mac_mini_m2
    environment:
      flutter: 3.22.3
    scripts:
      - name: Get packages
        script: flutter packages pub get
      - name: Build APK
        script: flutter build apk --debug
    artifacts:
      - build/app/outputs/flutter-apk/app-debug.apk
